# 백준 [3052 나머지]
```python
L = []                  #리스트 형태로 받기 위해
for _ in range(10):     #10번 받으므로
    n = int(input())    #숫자를 하나씩 받는다.
    L.append(n%42)      #42로 나눈 나머지를 L 리스트에 넣는다.
S = set(L)              #리스트를 세트로 바꾼다.(중복을 없애기 위해)
print(len(S))           #길이를 세면 개수를 세는 것이다.
```
