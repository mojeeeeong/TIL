# 백준 [2914 저작권]
```python
a, b = map(int, input().split())    #a : 앨범에 수록된 곡 / b : 평균값
ans = a*(b-1)                       #평균값은 올림하므로 하나 빼고 곱하면 제일 최저값이 나온다 (포함하지는 않음)
print(ans+1)                        #나온 멜로디에 1을 더하면 최소값이 나온다.
```
