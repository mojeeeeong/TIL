# 백준 [1436 영화감독]
```
N = int(input())

cnt = 0
num = 666

while 1:
    if '666' in str(num):
        cnt += 1

    if N == cnt:
        break

    num += 1

print(num)

'''
앞에 1부터 붙이거나 어느정도 숫자가 커지면 뒤에 또 숫자가 붙어지는 식으로 생각을 했는데
너무 복잡해진다. 다른 방법이 없을까 했지만 브루트 포스로 하나씩 더해가며 하는 식이 제일 나을 것 같다.
'''
```
